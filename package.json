{
    "name": "PS",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "copy-files": "copyfiles -u 1 src/**/*.html src/**/*.css src/**/*.js docs/",
        "clean": "rm -rf ./docs",
        "build": "yarn clean && tsc && yarn copy-files"
    },
    "devDependencies": {
        "@types/jquery": "^3.5.14",
        "copyfiles": "^2.4.1"
    }
}